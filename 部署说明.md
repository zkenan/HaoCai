# 耗材出入库管理系统 - 部署说明

## 📦 打包文件

**文件名**: `耗材管理系统.exe`  
**文件大小**: 约 53 MB  
**打包时间**: 2026-05-16

## 🚀 快速开始

### 1. 前置要求

在运行程序之前，请确保测试服务器已安装并配置好 **MySQL 数据库**。

### 2. 配置数据库连接

编辑 `database.json` 文件（与 .exe 文件在同一目录），修改为测试服务器的数据库配置：

```json
{
  "host": "测试服务器IP地址",
  "port": 3306,
  "database": "xapiaihaocai",
  "user": "数据库用户名",
  "password": "数据库密码"
}
```

**示例配置**：
```json
{
  "host": "192.168.1.100",
  "port": 3306,
  "database": "xapiaihaocai",
  "user": "root",
  "password": "your_password"
}
```

### 3. 运行程序

双击 `耗材管理系统.exe` 即可启动系统。

程序会自动：
- ✅ 检查数据库连接
- ✅ 创建数据库（如果不存在）
- ✅ 初始化表结构（如果是首次运行）
- ✅ 创建默认管理员账户

### 4. 访问系统

启动成功后，在浏览器中访问：

- **本地访问**: http://localhost:3000
- **局域网访问**: http://[本机IP]:3000

**默认管理员账户**：
- 用户名: `admin`
- 密码: `123456`

## 📋 功能清单

### ✅ 已实现功能

1. **用户认证**
   - 登录/登出
   - JWT Token 验证

2. **库存看板**
   - 实时库存统计
   - 低库存预警
   - 表格排序功能（点击表头正序/倒序）

3. **耗材管理**
   - 耗材列表展示
   - 新增/编辑/删除耗材
   - Excel 导入功能
   - 下载导入模板
   - 关键词搜索
   - 分页显示

4. **入库管理**
   - 创建入库单
   - 入库单列表
   - 入库单详情查看
   - 打印入库单（带AI Logo）
   - 自动生成入库单号

5. **出库管理**
   - 创建出库单
   - 出库单列表
   - 出库单详情查看
   - 打印出库单（带AI Logo）
   - 自动计算出库总价

6. **UI/UX**
   - 白色和蓝色主题（RGB 39, 107, 165）
   - 毛玻璃效果设计
   - 响应式布局
   - 菜单顺序优化

## 🔧 技术栈

- **前端**: Vue 3 + Element Plus + Vite
- **后端**: Node.js + Express
- **数据库**: MySQL
- **打包工具**: pkg

## ⚠️ 注意事项

### 1. 数据库要求

- MySQL 版本: 5.7 或更高
- 字符集: utf8mb4
- 数据库用户需要有 CREATE、ALTER 权限（首次运行时）

### 2. 端口占用

系统默认使用 **3000** 端口，如果端口被占用，可以通过环境变量修改：

```bash
set PORT=3001
耗材管理系统.exe
```

### 3. 防火墙设置

如果需要局域网访问，请确保：
- Windows 防火墙允许 3000 端口入站连接
- 路由器/交换机允许相应端口通信

### 4. 数据备份

建议定期备份数据库：

```bash
mysqldump -u [用户名] -p xapiaihaocai > backup_$(date +%Y%m%d).sql
```

### 5. 打印功能

- 打印功能需要浏览器允许弹出窗口
- 建议使用 Chrome、Edge 或 Firefox 浏览器
- 打印时可以选择保存为 PDF

## 🐛 常见问题

### Q1: 程序启动后提示"数据库连接失败"

**解决方案**：
1. 检查 MySQL 服务是否已启动
2. 确认 `database.json` 中的配置是否正确
3. 确认数据库用户有足够权限
4. 检查防火墙是否阻止了数据库连接

### Q2: 访问页面显示空白

**解决方案**：
1. 清除浏览器缓存（Ctrl+F5 强制刷新）
2. 检查浏览器控制台是否有错误信息
3. 确认程序是否正常启动（查看命令行输出）

### Q3: 下载模板按钮点击无反应

**解决方案**：
1. 检查浏览器是否阻止了下载
2. 查看浏览器控制台是否有错误
3. 确认网络连接正常

### Q4: 耗材管理页面数据显示错位

**解决方案**：
1. 此问题已在最新版本修复
2. 如果是旧数据库，需要执行以下 SQL：
   ```sql
   USE xapiaihaocai;
   ALTER TABLE consumables DROP COLUMN IF EXISTS total_price;
   ```
3. 重启程序

### Q5: 打印时看不到 Logo

**解决方案**：
1. Logo 采用 SVG 格式内嵌，应该正常显示
2. 检查浏览器是否支持 SVG
3. 尝试更换浏览器

## 📞 技术支持

如有问题，请联系系统管理员。

---

**版本**: 1.0.0  
**更新日期**: 2026-05-16  
**开发者**: AI Assistant
